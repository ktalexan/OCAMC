---
trigger: glob
globs: **/*.{js,css,md,tex}
---

# Web & Technical Writing
- **JavaScript:** Use ES6+ syntax and `camelCase`.
- **CSS:** Use `kebab-case` for class names.
- **LaTeX:** Use `amsmath` for math. Use spaces around operators in $inline$ and $$display$$ math.
